<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <include
        android:id="@+id/tool_bar"
        layout="@layout/toolbar" />

    <android.support.v4.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="?actionBarSize">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:paddingLeft="@dimen/new_case_left_and_right_padding"
            android:paddingRight="@dimen/new_case_left_and_right_padding"
            android:paddingTop="@dimen/top_padding"
            android:paddingBottom="@dimen/space_8">

            <include
                layout="@layout/medical_records_detail_view" />

            <Button
                android:id="@+id/new_medical_records_submit_button"
                android:layout_width="match_parent"
                android:layout_height="@dimen/new_case_button_height"
                android:layout_marginTop="@dimen/new_case_button_margin_top"
                android:text="@string/submit"
                android:textSize="@dimen/large_text_size"
                android:textColor="@color/colorWhite"
                android:background="@drawable/new_case_button" />
        </LinearLayout>

    </android.support.v4.widget.NestedScrollView>
</RelativeLayout>
